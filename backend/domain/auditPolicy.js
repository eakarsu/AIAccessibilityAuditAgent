'use strict';
const PRIVATE_HOST=/^(localhost|0\.0\.0\.0|127\.|10\.|192\.168\.|169\.254\.|::1$|fc|fd)/i;
function authorizeTarget(targetUrl,scopeHost){const violations=[];let parsed;try{parsed=new URL(targetUrl);}catch(_){return{valid:false,violations:['invalid_url']};}const host=parsed.hostname.toLowerCase(),scope=String(scopeHost||'').toLowerCase();if(!['https:','http:'].includes(parsed.protocol))violations.push('unsupported_protocol');if(!scope||(host!==scope&&!host.endsWith(`.${scope}`)))violations.push('outside_authorized_scope');if(PRIVATE_HOST.test(host)||host.endsWith('.local')||host.endsWith('.internal'))violations.push('private_target_blocked');if(parsed.username||parsed.password)violations.push('embedded_credentials_blocked');return{valid:violations.length===0,violations,normalizedUrl:parsed.toString()};}
function validateAuditEvidence(input){const target=authorizeTarget(input.targetUrl,input.authorizedHost),violations=[...target.violations];if(!input.authorizationReference||!input.sourceRevision)violations.push('authorization_and_revision_required');const run=input.axeRun||{};if(!run.runId||!run.engineVersion||!run.startedAt||!run.completedAt||!Array.isArray(run.findings))violations.push('reproducible_axe_run_required');for(const [i,f]of (run.findings||[]).entries()){if(!f.ruleId||!f.wcagCriterion||!['critical','serious','moderate','minor'].includes(f.impact)||!f.selector||!f.evidenceHash)violations.push(`finding_${i}_evidence_incomplete`);}return{valid:violations.length===0,violations,report:violations.length?null:{targetUrl:target.normalizedUrl,sourceRevision:input.sourceRevision,runId:run.runId,engineVersion:run.engineVersion,findingCount:run.findings.length,manualAssistiveTechnologyReviewRequired:true,automatedPassIsNotCertification:true}};}
module.exports={authorizeTarget,validateAuditEvidence};
